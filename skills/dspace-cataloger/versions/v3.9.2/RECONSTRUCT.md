# Reconstruct dspace-cataloger-v3.9.2.skill

Concatenate the files in `manifest.json` order, remove newlines, Base64-decode, then verify the resulting SHA-256 against the manifest. The artifact is a verified re-derivation from the supplied v3.9 source; it is not a binary successor of the failed v3.9.1 package.
